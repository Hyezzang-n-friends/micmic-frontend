import ProjectSerie from './ProjectSerie';
import styled from 'styled-components';

const StyledProjectSerie = styled(ProjectSerie)`
  .add__link {
    margin: 0 !important;
  }
`;

export default StyledProjectSerie;
